# Copy this file to `.env` before running the frontend locally and populate the secrets.
# The real `.env` file is ignored by git to prevent leaking credentials.
VITE_GOOGLE_CLIENT_ID=your-google-client-id
